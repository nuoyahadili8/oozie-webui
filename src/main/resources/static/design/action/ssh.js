$(document)
		.ready(
				
				function() {
					// $.getScript('../confSsh/selectForOption?date='
					// 		+ new Date(), function(data) {
					// 			var jsonData = eval(data);
					//
					// 	var str = "<option value='please choice'>请选择...</option>";
					// 	for ( var i = 0; i < jsonData.length; i++) {
					// 		str += "<option value=" + jsonData[i].ip + ">" + jsonData[i].name + "</option>";
					// 	}
					// 	$("select[id='host']").html(str);
					// });
				
				
				});


